import '@mlt/boomwhacker-sketchpad-ui/styles.css';
import { mountBoomwhackerSketchpad } from '@mlt/boomwhacker-sketchpad-ui';

const app = mountBoomwhackerSketchpad(document.getElementById('app')!);

export default app;
