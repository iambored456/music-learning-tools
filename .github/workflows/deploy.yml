# Deploy to GitHub Pages
#
# This workflow builds the hub multi-page bundle and assembles it into a single
# static site for GitHub Pages deployment.
#
# Output structure:
#   /                     -> hub (launcher)
#   /student-notation/    -> Student Notation app
#   /singing-trainer/     -> Singing Trainer app
#   /diatonic-compass/    -> Diatonic Compass app
#   /amateur-music-theory/ -> Amateur Music Theory (when added)

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:  # Allow manual trigger

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Validate app/package boundaries
        run: pnpm run check:architecture

      - name: Build hub bundle
        run: pnpm --filter hub run build
        env:
          NODE_ENV: production
          BASE_URL: /${{ github.event.repository.name }}/

      - name: Assemble Pages output
        run: node scripts/assemble-pages.js

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
